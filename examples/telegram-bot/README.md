# Telegram Bot

prereqs

1. download the rust docker image `docker pull rust`
2. get a telegram bot token via messaging `@BotFather` on telegram `/newbot`
3. innsert your token into the `telegram_bot/.env` and the `docker-compose.yaml` file
4.
